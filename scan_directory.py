import os
def scanFiles(directory):
	for root, _, files in os.walk
		for file in files:
		 file_path = os.path.join(root, file)
		 print(file_path)

if __name__ == "__main__":
	specified_folder = r""
	scanFiles(specified_foler)